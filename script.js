   /* document.addEventListener('DOMContentLoaded', function() {
      document.body.innerHTML = ''; // Clear the body

      const paragraph = document.createElement('p'); // Create a paragraph element
      paragraph.textContent = 'DOM load success'; // Set the text of the paragraph
      document.body.appendChild(paragraph); // Append the paragraph to the body
    });
*/